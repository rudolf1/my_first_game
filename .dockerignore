# Only dist/ and nginx.conf are needed inside the image.
# Everything else is excluded to keep the build context minimal.
node_modules
src
public
.git
.gitignore
*.log
.DS_Store
package.json
package-lock.json
tsconfig.json
index.html
README.md
